diff --git a/include/ZceGasSamples.h b/include/ZceGasSamples.h
new file mode 100644
index 0000000..959ffb2
--- /dev/null
+++ b/include/ZceGasSamples.h
@@ -0,0 +1,39 @@
+#pragma once
+#include <stdint.h>
+
+// One validated snapshot per second, with five samples for each ZCE04B gas.
+class ZceGasSamples {
+ public:
+  enum Gas { CO, H2S, O2, CH4, GAS_COUNT };
+  static constexpr uint32_t SAMPLE_INTERVAL_MS = 1000;
+  static constexpr uint8_t SAMPLE_COUNT = 5;
+
+  bool sampleDue(uint32_t nowMs) {
+    if (uint32_t(nowMs - lastSampleMs_) < SAMPLE_INTERVAL_MS) return false;
+    lastSampleMs_ = nowMs;
+    return true;
+  }
+
+  void reset() { next_ = 0; count_ = 0; }
+
+  void add(float co, float h2s, float o2, float ch4) {
+    const float values[GAS_COUNT] = {co, h2s, o2, ch4};
+    for (uint8_t gas = 0; gas < GAS_COUNT; ++gas) samples_[gas][next_] = values[gas];
+    next_ = (next_ + 1) % SAMPLE_COUNT;
+    if (count_ < SAMPLE_COUNT) ++count_;
+  }
+
+  bool ready() const { return count_ == SAMPLE_COUNT; }
+  float average(Gas gas) const {
+    if (count_ == 0) return 0.0f;
+    float total = 0.0f;
+    for (uint8_t i = 0; i < count_; ++i) total += samples_[gas][i];
+    return total / count_;
+  }
+
+ private:
+  float samples_[GAS_COUNT][SAMPLE_COUNT] = {};
+  uint32_t lastSampleMs_ = 0;
+  uint8_t next_ = 0;
+  uint8_t count_ = 0;
+};
diff --git a/origin.cpp b/origin.cpp
index 6c0c429..e0c03e9 100644
--- a/origin.cpp
+++ b/origin.cpp
@@ -1,3 +1,6 @@
+#include "include/ZceGasSamples.h"
+static ZceGasSamples zceGasSamples;
+
 #include <Arduino.h>
 #include <Audio.h>
 #include <LittleFS.h>
@@ -3120,21 +3123,21 @@ bool sendToServerAndRemember() {
 bool isGasDangerAlertCondition() {
   return
       (co2_val >= 15000.0f) ||
-      (ch4_val >= 1.5f) ||
-      (o2_val < 19.5f) ||
-      (o2_val > 23.6f) ||
-      (co_val >= 60.0f) ||
-      (h2s_val >= 20.0f);
+      ((zceGasSamples.ready() && zceGasSamples.average(ZceGasSamples::CH4) >= 1.5f)) ||
+      ((zceGasSamples.ready() && zceGasSamples.average(ZceGasSamples::O2) < 19.5f)) ||
+      ((zceGasSamples.ready() && zceGasSamples.average(ZceGasSamples::O2) > 23.6f)) ||
+      ((zceGasSamples.ready() && zceGasSamples.average(ZceGasSamples::CO) >= 60.0f)) ||
+      ((zceGasSamples.ready() && zceGasSamples.average(ZceGasSamples::H2S) >= 20.0f));
 }
 
 bool shouldSendToServerForThreshold() {
   return
       (co2_val >= serverSendThresholds.co2) ||
-      (ch4_val >= serverSendThresholds.ch4) ||
-      (o2_val < serverSendThresholds.o2Low) ||
-      (o2_val > serverSendThresholds.o2High) ||
-      (co_val >= serverSendThresholds.co) ||
-      (h2s_val >= serverSendThresholds.h2s) ||
+      ((zceGasSamples.ready() && zceGasSamples.average(ZceGasSamples::CH4) >= serverSendThresholds.ch4)) ||
+      ((zceGasSamples.ready() && zceGasSamples.average(ZceGasSamples::O2) < serverSendThresholds.o2Low)) ||
+      ((zceGasSamples.ready() && zceGasSamples.average(ZceGasSamples::O2) > serverSendThresholds.o2High)) ||
+      ((zceGasSamples.ready() && zceGasSamples.average(ZceGasSamples::CO) >= serverSendThresholds.co)) ||
+      ((zceGasSamples.ready() && zceGasSamples.average(ZceGasSamples::H2S) >= serverSendThresholds.h2s)) ||
       (temperature >= serverSendThresholds.tempHigh) ||
       (temperature <= serverSendThresholds.tempLow);
 }
@@ -3155,13 +3158,13 @@ void addAlertTrack(uint8_t* tracks, uint8_t& count, uint8_t trackIndex) {
 void buildAlertTrackQueue(uint8_t* tracks, uint8_t& count) {
   count = 0;
 
-  if (o2_val < 19.5f || o2_val > 23.6f) {
+  if ((zceGasSamples.ready() && zceGasSamples.average(ZceGasSamples::O2) < 19.5f) || (zceGasSamples.ready() && zceGasSamples.average(ZceGasSamples::O2) > 23.6f)) {
     addAlertTrack(tracks, count, 3);   // 0003.mp3: oxygen alert
   }
   if ((co2_val >= 15000.0f) ||
-      (ch4_val >= 1.5f) ||
-      (co_val >= 60.0f) ||
-      (h2s_val >= 20.0f)) {
+      ((zceGasSamples.ready() && zceGasSamples.average(ZceGasSamples::CH4) >= 1.5f)) ||
+      ((zceGasSamples.ready() && zceGasSamples.average(ZceGasSamples::CO) >= 60.0f)) ||
+      ((zceGasSamples.ready() && zceGasSamples.average(ZceGasSamples::H2S) >= 20.0f))) {
     addAlertTrack(tracks, count, 5);   // 0005.mp3: hazardous gas alert
   }
   if (temperature >= 99.0f) {
@@ -3400,6 +3403,7 @@ void calibrateZceZero(const char* gasName, const uint8_t* switchCommand) {
 }
 
 void calibrateO2AirSpan20_9() {
+  zceGasSamples.reset();
   Serial.println("O2 air span calibration start");
   switchZceGas(ZCE_SWITCH_TO_O2);
   sendZceCommand(ZCE_O2_AIR_SPAN_20_9, ZCE_CMD_LEN);
@@ -3407,6 +3411,7 @@ void calibrateO2AirSpan20_9() {
 }
 
 void calibrateAllGasInCleanAir() {
+  zceGasSamples.reset();
   Serial.println("All gas calibration start");
   Serial.println("Keep the sensor in clean normal air. Target: O2=20.9%, CO=0ppm, H2S=0ppm, CH4=0%LEL");
 
@@ -3420,6 +3425,8 @@ void calibrateAllGasInCleanAir() {
 }
 
 bool readZCE04B() {
+  if (!zceGasSamples.sampleDue(millis())) return false;
+  bool received = false;
   selectGasUart();
   // 시리얼 버퍼에서 0xFF(스타트) 맞춰서 읽기
   while (gasSerial.available() > 0) {
@@ -3430,11 +3437,11 @@ bool readZCE04B() {
     }
 
     // 프레임 길이만큼 안 쌓였으면 다음 loop에서
-    if (gasSerial.available() < ZCE_FRAME_LEN) return false;
+    if (gasSerial.available() < ZCE_FRAME_LEN) break;
 
     // 11바이트 읽기
     size_t n = gasSerial.readBytes(zceFrame, ZCE_FRAME_LEN);
-    if (n != ZCE_FRAME_LEN) return false;
+    if (n != ZCE_FRAME_LEN) break;
 
     // 기본 검증
     if (zceFrame[0] != 0xFF) continue;
@@ -3460,10 +3467,11 @@ bool readZCE04B() {
     o2_val  = float(o2_raw) * 0.1f;   // %vol
     ch4_val = float(ch4_raw);         // %LEL
 
-    return true;
+    received = true;
   }
 
-  return false;
+  if (received) zceGasSamples.add(co_val, h2s_val, o2_val, ch4_val);
+  return received;
 }
 
 // void readZCE04B() {  //다른 센서 버전 
@@ -3516,6 +3524,7 @@ void readSCD41() {
 unsigned long lastUpdate = 0;
 bool sensorSnapshotReady = false;
 void loop() {
+  readZCE04B();  // Independent 1-second gas sampling, before slower alert/network work.
   handleSerialConfigInput();
   const uint32_t nowMs = millis();
   serviceAudioLoop();
@@ -3542,9 +3551,9 @@ void loop() {
   serviceSosSequence(nowMs);
   serviceSensorAlertSequence(nowMs);
  
-  if (millis() - lastUpdate >= SENSOR_ALERT_REPEAT_MS) {
+  if (millis() - lastUpdate >= ZceGasSamples::SAMPLE_INTERVAL_MS) {
     
-  readZCE04B();
+
   readSCD41();
     bool warn = false;
   
@@ -3583,7 +3592,7 @@ void loop() {
     if(false){
       
             digitalWrite(siren_led, LOW);
-      if (o2_val < 19.5f || o2_val > 23.6f) {
+      if ((zceGasSamples.ready() && zceGasSamples.average(ZceGasSamples::O2) < 19.5f) || (zceGasSamples.ready() && zceGasSamples.average(ZceGasSamples::O2) > 23.6f)) {
             warn = true;
             
             digitalWrite(siren_led, HIGH);
@@ -3599,9 +3608,9 @@ void loop() {
           }
           else if (
             (co2_val >=15000.0f) ||
-            (ch4_val >= 1.5f) ||
-            (co_val >= 60.0f) ||
-            (h2s_val >= 20.0f)
+            ((zceGasSamples.ready() && zceGasSamples.average(ZceGasSamples::CH4) >= 1.5f)) ||
+            ((zceGasSamples.ready() && zceGasSamples.average(ZceGasSamples::CO) >= 60.0f)) ||
+            ((zceGasSamples.ready() && zceGasSamples.average(ZceGasSamples::H2S) >= 20.0f))
           ) {
             warn = true;
             
diff --git a/src/main.cpp b/src/main.cpp
index 969848d..6385f24 100644
--- a/src/main.cpp
+++ b/src/main.cpp
@@ -1,4 +1,7 @@
-﻿#include <Arduino.h>
+﻿#include "../include/ZceGasSamples.h"
+static ZceGasSamples zceGasSamples;
+
+#include <Arduino.h>
 #include <Audio.h>
 #include <LittleFS.h>
 #include <Wire.h>
@@ -3414,20 +3417,20 @@ bool sendToServerAndRemember() {
 bool isGasDangerAlertCondition() {
   return
       (co2_val >= 15000.0f) ||
-      (ch4_val >= 1.5f) ||
-      (o2MonitoringReady && (o2_val < 19.5f || o2_val > 23.6f)) ||
-      (co_val >= 60.0f) ||
-      (h2s_val >= 20.0f);
+      ((zceGasSamples.ready() && zceGasSamples.average(ZceGasSamples::CH4) >= 1.5f)) ||
+      (o2MonitoringReady && ((zceGasSamples.ready() && zceGasSamples.average(ZceGasSamples::O2) < 19.5f) || (zceGasSamples.ready() && zceGasSamples.average(ZceGasSamples::O2) > 23.6f))) ||
+      ((zceGasSamples.ready() && zceGasSamples.average(ZceGasSamples::CO) >= 60.0f)) ||
+      ((zceGasSamples.ready() && zceGasSamples.average(ZceGasSamples::H2S) >= 20.0f));
 }
 
 bool shouldSendToServerForThreshold() {
   return
       (co2_val >= serverSendThresholds.co2) ||
-      (ch4_val >= serverSendThresholds.ch4) ||
-      (o2MonitoringReady && (o2_val < serverSendThresholds.o2Low ||
-                             o2_val > serverSendThresholds.o2High)) ||
-      (co_val >= serverSendThresholds.co) ||
-      (h2s_val >= serverSendThresholds.h2s) ||
+      ((zceGasSamples.ready() && zceGasSamples.average(ZceGasSamples::CH4) >= serverSendThresholds.ch4)) ||
+      (o2MonitoringReady && ((zceGasSamples.ready() && zceGasSamples.average(ZceGasSamples::O2) < serverSendThresholds.o2Low) ||
+                             (zceGasSamples.ready() && zceGasSamples.average(ZceGasSamples::O2) > serverSendThresholds.o2High))) ||
+      ((zceGasSamples.ready() && zceGasSamples.average(ZceGasSamples::CO) >= serverSendThresholds.co)) ||
+      ((zceGasSamples.ready() && zceGasSamples.average(ZceGasSamples::H2S) >= serverSendThresholds.h2s)) ||
       (temperature >= serverSendThresholds.tempHigh) ||
       (temperature <= serverSendThresholds.tempLow);
 }
@@ -3448,13 +3451,13 @@ void addAlertTrack(uint8_t* tracks, uint8_t& count, uint8_t trackIndex) {
 void buildAlertTrackQueue(uint8_t* tracks, uint8_t& count) {
   count = 0;
 
-  if (o2MonitoringReady && (o2_val < 19.5f || o2_val > 23.6f)) {
+  if (o2MonitoringReady && ((zceGasSamples.ready() && zceGasSamples.average(ZceGasSamples::O2) < 19.5f) || (zceGasSamples.ready() && zceGasSamples.average(ZceGasSamples::O2) > 23.6f))) {
     addAlertTrack(tracks, count, 3);   // 0003.mp3: oxygen alert
   }
   if ((co2_val >= 15000.0f) ||
-      (ch4_val >= 1.5f) ||
-      (co_val >= 60.0f) ||
-      (h2s_val >= 20.0f)) {
+      ((zceGasSamples.ready() && zceGasSamples.average(ZceGasSamples::CH4) >= 1.5f)) ||
+      ((zceGasSamples.ready() && zceGasSamples.average(ZceGasSamples::CO) >= 60.0f)) ||
+      ((zceGasSamples.ready() && zceGasSamples.average(ZceGasSamples::H2S) >= 20.0f))) {
     addAlertTrack(tracks, count, 5);   // 0005.mp3: hazardous gas alert
   }
   if (temperature >= 99.0f) {
@@ -3699,6 +3702,7 @@ void calibrateZceZero(const char* gasName, const uint8_t* switchCommand) {
 }
 
 void calibrateO2AirSpan20_9() {
+  zceGasSamples.reset();
   Serial.println("O2 air span calibration start");
   switchZceGas(ZCE_SWITCH_TO_O2);
   sendZceCommand(ZCE_O2_AIR_SPAN_20_9, ZCE_CMD_LEN);
@@ -3706,6 +3710,7 @@ void calibrateO2AirSpan20_9() {
 }
 
 void calibrateAllGasInCleanAir() {
+  zceGasSamples.reset();
   Serial.println("All gas calibration start");
   Serial.println("Keep the sensor in clean normal air. Target: O2=20.9%, CO=0ppm, H2S=0ppm, CH4=0%LEL");
 
@@ -3719,6 +3724,8 @@ void calibrateAllGasInCleanAir() {
 }
 
 bool readZCE04B() {
+  if (!zceGasSamples.sampleDue(millis())) return false;
+  bool received = false;
   selectGasUart();
   // ?�리??버퍼?�서 0xFF(?��??? 맞춰???�기
   while (gasSerial.available() > 0) {
@@ -3729,11 +3736,11 @@ bool readZCE04B() {
     }
 
     // ?�레??길이만큼 ???��??�면 ?�음 loop?�서
-    if (gasSerial.available() < ZCE_FRAME_LEN) return false;
+    if (gasSerial.available() < ZCE_FRAME_LEN) break;
 
     // 11바이???�기
     size_t n = gasSerial.readBytes(zceFrame, ZCE_FRAME_LEN);
-    if (n != ZCE_FRAME_LEN) return false;
+    if (n != ZCE_FRAME_LEN) break;
 
     // 기본 검�?
     if (zceFrame[0] != 0xFF) continue;
@@ -3763,10 +3770,11 @@ bool readZCE04B() {
       Serial.printf("[O2] monitoring started at %.1f%%\n", o2_val);
     }
 
-    return true;
+    received = true;
   }
 
-  return false;
+  if (received) zceGasSamples.add(co_val, h2s_val, o2_val, ch4_val);
+  return received;
 }
 
 // void readZCE04B() {  //?�른 ?�서 버전 
@@ -3819,6 +3827,7 @@ void readSCD41() {
 unsigned long lastUpdate = 0;
 bool sensorSnapshotReady = false;
 void loop() {
+  readZCE04B();  // Independent 1-second gas sampling, before slower alert/network work.
   if (ENABLE_AUDIO_VOLUME_TEST) {
     serviceAudioVolumeTest(millis());
     return;
@@ -3850,9 +3859,9 @@ void loop() {
   serviceSosSequence(nowMs);
   serviceSensorAlertSequence(nowMs);
  
-  if (millis() - lastUpdate >= SENSOR_ALERT_REPEAT_MS) {
+  if (millis() - lastUpdate >= ZceGasSamples::SAMPLE_INTERVAL_MS) {
     
-  readZCE04B();
+
   readSCD41();
     bool warn = false;
   
@@ -3886,7 +3895,7 @@ void loop() {
     if(false){
       
             digitalWrite(siren_led, LOW);
-      if (o2_val < 19.5f || o2_val > 23.6f) {
+      if ((zceGasSamples.ready() && zceGasSamples.average(ZceGasSamples::O2) < 19.5f) || (zceGasSamples.ready() && zceGasSamples.average(ZceGasSamples::O2) > 23.6f)) {
             warn = true;
             
             digitalWrite(siren_led, HIGH);
@@ -3897,9 +3906,9 @@ void loop() {
           }
           else if (
             (co2_val >=15000.0f) ||
-            (ch4_val >= 1.5f) ||
-            (co_val >= 60.0f) ||
-            (h2s_val >= 20.0f)
+            ((zceGasSamples.ready() && zceGasSamples.average(ZceGasSamples::CH4) >= 1.5f)) ||
+            ((zceGasSamples.ready() && zceGasSamples.average(ZceGasSamples::CO) >= 60.0f)) ||
+            ((zceGasSamples.ready() && zceGasSamples.average(ZceGasSamples::H2S) >= 20.0f))
           ) {
             warn = true;
             
diff --git a/test/broad_cast_only.cpp b/test/broad_cast_only.cpp
index 16bb0d0..6debe18 100644
--- a/test/broad_cast_only.cpp
+++ b/test/broad_cast_only.cpp
@@ -1,3 +1,6 @@
+#include "../include/ZceGasSamples.h"
+static ZceGasSamples zceGasSamples;
+
 #include <Arduino.h>
 #include <Audio.h>
 #include <LittleFS.h>
@@ -1969,21 +1972,21 @@ bool sendToServerAndRemember() {
 bool isGasDangerAlertCondition() {
   return
       (co2_val >= 15000.0f) ||
-      (ch4_val >= 1.5f) ||
-      (o2_val < 19.5f) ||
-      (o2_val > 23.6f) ||
-      (co_val >= 60.0f) ||
-      (h2s_val >= 20.0f);
+      ((zceGasSamples.ready() && zceGasSamples.average(ZceGasSamples::CH4) >= 1.5f)) ||
+      ((zceGasSamples.ready() && zceGasSamples.average(ZceGasSamples::O2) < 19.5f)) ||
+      ((zceGasSamples.ready() && zceGasSamples.average(ZceGasSamples::O2) > 23.6f)) ||
+      ((zceGasSamples.ready() && zceGasSamples.average(ZceGasSamples::CO) >= 60.0f)) ||
+      ((zceGasSamples.ready() && zceGasSamples.average(ZceGasSamples::H2S) >= 20.0f));
 }
 
 bool shouldSendToServerForThreshold() {
   return
       (co2_val >= serverSendThresholds.co2) ||
-      (ch4_val >= serverSendThresholds.ch4) ||
-      (o2_val < serverSendThresholds.o2Low) ||
-      (o2_val > serverSendThresholds.o2High) ||
-      (co_val >= serverSendThresholds.co) ||
-      (h2s_val >= serverSendThresholds.h2s) ||
+      ((zceGasSamples.ready() && zceGasSamples.average(ZceGasSamples::CH4) >= serverSendThresholds.ch4)) ||
+      ((zceGasSamples.ready() && zceGasSamples.average(ZceGasSamples::O2) < serverSendThresholds.o2Low)) ||
+      ((zceGasSamples.ready() && zceGasSamples.average(ZceGasSamples::O2) > serverSendThresholds.o2High)) ||
+      ((zceGasSamples.ready() && zceGasSamples.average(ZceGasSamples::CO) >= serverSendThresholds.co)) ||
+      ((zceGasSamples.ready() && zceGasSamples.average(ZceGasSamples::H2S) >= serverSendThresholds.h2s)) ||
       (temperature >= serverSendThresholds.tempHigh) ||
       (temperature <= serverSendThresholds.tempLow);
 }
@@ -2156,6 +2159,7 @@ void calibrateZceZero(const char* gasName, const uint8_t* switchCommand) {
 }
 
 void calibrateO2AirSpan20_9() {
+  zceGasSamples.reset();
   Serial.println("O2 air span calibration start");
   switchZceGas(ZCE_SWITCH_TO_O2);
   sendZceCommand(ZCE_O2_AIR_SPAN_20_9, ZCE_CMD_LEN);
@@ -2163,6 +2167,7 @@ void calibrateO2AirSpan20_9() {
 }
 
 void calibrateAllGasInCleanAir() {
+  zceGasSamples.reset();
   Serial.println("All gas calibration start");
   Serial.println("Keep the sensor in clean normal air. Target: O2=20.9%, CO=0ppm, H2S=0ppm, CH4=0%LEL");
 
@@ -2177,6 +2182,8 @@ void calibrateAllGasInCleanAir() {
 
 // ?�레??1�??�싱 ?�공?�면 true
 bool readZCE04B() {
+  if (!zceGasSamples.sampleDue(millis())) return false;
+  bool received = false;
   selectGasUart();
   // ?�리??버퍼?�서 0xFF(?��??? 맞춰???�기
   while (gasSerial.available() > 0) {
@@ -2187,11 +2194,11 @@ bool readZCE04B() {
     }
 
     // ?�레??길이만큼 ???��??�면 ?�음 loop?�서
-    if (gasSerial.available() < ZCE_FRAME_LEN) return false;
+    if (gasSerial.available() < ZCE_FRAME_LEN) break;
 
     // 11바이???�기
     size_t n = gasSerial.readBytes(zceFrame, ZCE_FRAME_LEN);
-    if (n != ZCE_FRAME_LEN) return false;
+    if (n != ZCE_FRAME_LEN) break;
 
     // 기본 검�?
     if (zceFrame[0] != 0xFF) continue;
@@ -2217,10 +2224,11 @@ bool readZCE04B() {
     o2_val  = float(o2_raw) * 0.1f;   // %vol
     ch4_val = float(ch4_raw);         // %LEL
 
-    return true;
+    received = true;
   }
 
-  return false;
+  if (received) zceGasSamples.add(co_val, h2s_val, o2_val, ch4_val);
+  return received;
 }
 
 // void readZCE04B() {  //?�른 ?�서 버전 
@@ -2272,7 +2280,22 @@ void readSCD41() {
 // 측정 주기
 unsigned long lastUpdate = 0;
 bool sensorSnapshotReady = false;
+// Sampling and alarm evaluation run each second; audio repeats every five seconds.
+void playSampledSensorAlert(uint8_t track) {
+  static bool played = false;
+  static uint8_t lastTrack = 0;
+  static uint32_t lastPlaybackMs = 0;
+  const uint32_t nowMs = millis();
+  if (!played || track != lastTrack || uint32_t(nowMs - lastPlaybackMs) >= 5000) {
+    audioPlayByIndex(track);
+    played = true;
+    lastTrack = track;
+    lastPlaybackMs = nowMs;
+  }
+}
+
 void loop() {
+  readZCE04B();  // Independent 1-second gas sampling, before slower alert/network work.
   serviceAudioLoop();
 #if ETHERNET_USE_DHCP
   if (netMode == ETHERNET_MODE) {
@@ -2298,9 +2321,9 @@ void loop() {
   }
   serviceSosSequence(nowMs);
  
-  if (millis() - lastUpdate >= SENSOR_ALERT_REPEAT_MS) {
+  if (millis() - lastUpdate >= ZceGasSamples::SAMPLE_INTERVAL_MS) {
     
-  readZCE04B();
+
   readSCD41();
     bool warn = false;
     bool gasDangerAlert = false;
@@ -2312,7 +2335,7 @@ void loop() {
     if(sosState==0){
       
             digitalWrite(siren_led, LOW);
-      if (o2_val < 19.5f || o2_val > 23.6f) {
+      if ((zceGasSamples.ready() && zceGasSamples.average(ZceGasSamples::O2) < 19.5f) || (zceGasSamples.ready() && zceGasSamples.average(ZceGasSamples::O2) > 23.6f)) {
             warn = true;
             
             digitalWrite(siren_led, HIGH);
@@ -2323,14 +2346,14 @@ void loop() {
               delay(300);
 
             }
-            audioPlayByIndex(3);   // 0003.mp3: ?�소부�?
+            playSampledSensorAlert(3);   // 0003.mp3: ?�소부�?
             //play_music();
           }
           else if (
             (co2_val >=15000.0f) ||
-            (ch4_val >= 1.5f) ||
-            (co_val >= 60.0f) ||
-            (h2s_val >= 20.0f)
+            ((zceGasSamples.ready() && zceGasSamples.average(ZceGasSamples::CH4) >= 1.5f)) ||
+            ((zceGasSamples.ready() && zceGasSamples.average(ZceGasSamples::CO) >= 60.0f)) ||
+            ((zceGasSamples.ready() && zceGasSamples.average(ZceGasSamples::H2S) >= 20.0f))
           ) {
             warn = true;
             
@@ -2341,7 +2364,7 @@ void loop() {
               delay(300);
 
             }
-            audioPlayByIndex(5);   // 0005.mp3: ?�해가?�감지
+            playSampledSensorAlert(5);   // 0005.mp3: ?�해가?�감지
             //play_music();
           }  
           else if (temperature >= 99 ) {
@@ -2354,7 +2377,7 @@ void loop() {
               delay(300);
 
             }
-            audioPlayByIndex(4);   // 0004.mp3:고온
+            playSampledSensorAlert(4);   // 0004.mp3:고온
             //play_music();
           }
           else if (temperature <-10 ) {
@@ -2367,7 +2390,7 @@ void loop() {
               delay(300);
 
             }
-            audioPlayByIndex(2);   // 0002.mp3:?�??
+            playSampledSensorAlert(2);   // 0002.mp3:?�??
             //play_music();
           }
           else if (batteryVoltage <= 10 ) {
@@ -2380,7 +2403,7 @@ void loop() {
             }
             if (battery_check_time == 0 || nowMs - battery_check_time >= LOW_BATTERY_ALERT_INTERVAL_MS) {
               battery_check_time = nowMs;
-              audioPlayByIndex(9);   // 0009.mp3:?�?�압
+              playSampledSensorAlert(9);   // 0009.mp3:?�?�압
             }
             //play_music();
           }
diff --git a/test/calibration.cpp b/test/calibration.cpp
index df69fc5..a34ce3d 100644
--- a/test/calibration.cpp
+++ b/test/calibration.cpp
@@ -1,3 +1,6 @@
+#include "../include/ZceGasSamples.h"
+static ZceGasSamples zceGasSamples;
+
 #include <Arduino.h>
 #include <EthernetENC.h>
 #include <EthernetUdp.h>
@@ -115,6 +118,7 @@ void calibrateZceZero(const char* gasName, const uint8_t* switchCommand) {
 }
 
 void calibrateO2AirSpan20_9() {
+  zceGasSamples.reset();
   Serial.println("O2 air span calibration start");
   switchZceGas(ZCE_SWITCH_TO_O2);
   sendZceCommand(ZCE_O2_AIR_SPAN_20_9, ZCE_CMD_LEN);
@@ -122,6 +126,7 @@ void calibrateO2AirSpan20_9() {
 }
 
 void calibrateAllGasInCleanAir() {
+  zceGasSamples.reset();
   Serial.println("All gas calibration start");
   Serial.println("Keep the sensor in clean normal air. Target: O2=20.9%, CO=0ppm, H2S=0ppm, CH4=0%LEL");
 
@@ -159,6 +164,8 @@ void handleSerialCommand() {
 }
 
 bool readZCE04B() {
+  if (!zceGasSamples.sampleDue(millis())) return false;
+  bool received = false;
   while (gasSerial.available() > 0) {
     if (gasSerial.peek() != 0xFF) {
       gasSerial.read();
@@ -166,12 +173,12 @@ bool readZCE04B() {
     }
 
     if (gasSerial.available() < ZCE_FRAME_LEN) {
-      return false;
+      break;
     }
 
     const size_t n = gasSerial.readBytes(zceFrame, ZCE_FRAME_LEN);
     if (n != ZCE_FRAME_LEN) {
-      return false;
+      break;
     }
 
     if (zceFrame[0] != 0xFF || zceFrame[1] != 0x86) {
@@ -193,18 +200,19 @@ bool readZCE04B() {
     o2_val = float(o2_raw) * 0.1f;   // %vol
     ch4_val = float(ch4_raw);        // %LEL
 
-    return true;
+    received = true;
   }
 
-  return false;
+  if (received) zceGasSamples.add(co_val, h2s_val, o2_val, ch4_val);
+  return received;
 }
 
 bool isGasDanger() {
-  return o2_val < 19.5f ||
-         o2_val > 23.6f ||
-         ch4_val >= 1.5f ||
-         co_val >= 60.0f ||
-         h2s_val >= 20.0f;
+  return (zceGasSamples.ready() && zceGasSamples.average(ZceGasSamples::O2) < 19.5f) ||
+         (zceGasSamples.ready() && zceGasSamples.average(ZceGasSamples::O2) > 23.6f) ||
+         (zceGasSamples.ready() && zceGasSamples.average(ZceGasSamples::CH4) >= 1.5f) ||
+         (zceGasSamples.ready() && zceGasSamples.average(ZceGasSamples::CO) >= 60.0f) ||
+         (zceGasSamples.ready() && zceGasSamples.average(ZceGasSamples::H2S) >= 20.0f);
 }
 
 void printGasValues() {
diff --git a/test/dhcp_ethernet.cpp b/test/dhcp_ethernet.cpp
index 65b429e..5077e67 100644
--- a/test/dhcp_ethernet.cpp
+++ b/test/dhcp_ethernet.cpp
@@ -1,3 +1,6 @@
+#include "../include/ZceGasSamples.h"
+static ZceGasSamples zceGasSamples;
+
 #include <Arduino.h>
 #include <Wire.h>
 #include <SPI.h>
@@ -3167,21 +3170,21 @@ bool sendToServerAndRemember() {
 bool isGasDangerAlertCondition() {
   return
       (co2_val >= 15000.0f) ||
-      (ch4_val >= 1.5f) ||
-      (o2_val < 19.5f) ||
-      (o2_val > 23.6f) ||
-      (co_val >= 60.0f) ||
-      (h2s_val >= 20.0f);
+      ((zceGasSamples.ready() && zceGasSamples.average(ZceGasSamples::CH4) >= 1.5f)) ||
+      ((zceGasSamples.ready() && zceGasSamples.average(ZceGasSamples::O2) < 19.5f)) ||
+      ((zceGasSamples.ready() && zceGasSamples.average(ZceGasSamples::O2) > 23.6f)) ||
+      ((zceGasSamples.ready() && zceGasSamples.average(ZceGasSamples::CO) >= 60.0f)) ||
+      ((zceGasSamples.ready() && zceGasSamples.average(ZceGasSamples::H2S) >= 20.0f));
 }
 
 bool shouldSendToServerForThreshold() {
   return
       (co2_val >= serverSendThresholds.co2) ||
-      (ch4_val >= serverSendThresholds.ch4) ||
-      (o2_val < serverSendThresholds.o2Low) ||
-      (o2_val > serverSendThresholds.o2High) ||
-      (co_val >= serverSendThresholds.co) ||
-      (h2s_val >= serverSendThresholds.h2s) ||
+      ((zceGasSamples.ready() && zceGasSamples.average(ZceGasSamples::CH4) >= serverSendThresholds.ch4)) ||
+      ((zceGasSamples.ready() && zceGasSamples.average(ZceGasSamples::O2) < serverSendThresholds.o2Low)) ||
+      ((zceGasSamples.ready() && zceGasSamples.average(ZceGasSamples::O2) > serverSendThresholds.o2High)) ||
+      ((zceGasSamples.ready() && zceGasSamples.average(ZceGasSamples::CO) >= serverSendThresholds.co)) ||
+      ((zceGasSamples.ready() && zceGasSamples.average(ZceGasSamples::H2S) >= serverSendThresholds.h2s)) ||
       (temperature >= serverSendThresholds.tempHigh) ||
       (temperature <= serverSendThresholds.tempLow);
 }
@@ -3354,6 +3357,7 @@ void calibrateZceZero(const char* gasName, const uint8_t* switchCommand) {
 }
 
 void calibrateO2AirSpan20_9() {
+  zceGasSamples.reset();
   Serial.println("O2 air span calibration start");
   switchZceGas(ZCE_SWITCH_TO_O2);
   sendZceCommand(ZCE_O2_AIR_SPAN_20_9, ZCE_CMD_LEN);
@@ -3361,6 +3365,7 @@ void calibrateO2AirSpan20_9() {
 }
 
 void calibrateAllGasInCleanAir() {
+  zceGasSamples.reset();
   Serial.println("All gas calibration start");
   Serial.println("Keep the sensor in clean normal air. Target: O2=20.9%, CO=0ppm, H2S=0ppm, CH4=0%LEL");
 
@@ -3375,6 +3380,8 @@ void calibrateAllGasInCleanAir() {
 
 // ?�레??1�??�싱 ?�공?�면 true
 bool readZCE04B() {
+  if (!zceGasSamples.sampleDue(millis())) return false;
+  bool received = false;
   selectGasUart();
   // ?�리??버퍼?�서 0xFF(?��??? 맞춰???�기
   while (gasSerial.available() > 0) {
@@ -3385,11 +3392,11 @@ bool readZCE04B() {
     }
 
     // ?�레??길이만큼 ???��??�면 ?�음 loop?�서
-    if (gasSerial.available() < ZCE_FRAME_LEN) return false;
+    if (gasSerial.available() < ZCE_FRAME_LEN) break;
 
     // 11바이???�기
     size_t n = gasSerial.readBytes(zceFrame, ZCE_FRAME_LEN);
-    if (n != ZCE_FRAME_LEN) return false;
+    if (n != ZCE_FRAME_LEN) break;
 
     // 기본 검�?
     if (zceFrame[0] != 0xFF) continue;
@@ -3415,10 +3422,11 @@ bool readZCE04B() {
     o2_val  = float(o2_raw) * 0.1f;   // %vol
     ch4_val = float(ch4_raw);         // %LEL
 
-    return true;
+    received = true;
   }
 
-  return false;
+  if (received) zceGasSamples.add(co_val, h2s_val, o2_val, ch4_val);
+  return received;
 }
 
 // void readZCE04B() {  //?�른 ?�서 버전 
@@ -3470,7 +3478,22 @@ void readSCD41() {
 // 측정 주기
 unsigned long lastUpdate = 0;
 bool sensorSnapshotReady = false;
+// Sampling and alarm evaluation run each second; audio repeats every five seconds.
+void playSampledSensorAlert(uint8_t track) {
+  static bool played = false;
+  static uint8_t lastTrack = 0;
+  static uint32_t lastPlaybackMs = 0;
+  const uint32_t nowMs = millis();
+  if (!played || track != lastTrack || uint32_t(nowMs - lastPlaybackMs) >= 5000) {
+    audioPlayByIndex(track);
+    played = true;
+    lastTrack = track;
+    lastPlaybackMs = nowMs;
+  }
+}
+
 void loop() {
+  readZCE04B();  // Independent 1-second gas sampling, before slower alert/network work.
 #if ETHERNET_USE_DHCP
   if (netMode == ETHERNET_MODE) {
     if (!ethernetUsingStaticIp) {
@@ -3496,9 +3519,9 @@ void loop() {
   }
   serviceSosSequence(nowMs);
  
-  if (millis() - lastUpdate >= SENSOR_ALERT_REPEAT_MS) {
+  if (millis() - lastUpdate >= ZceGasSamples::SAMPLE_INTERVAL_MS) {
     
-  readZCE04B();
+
   readSCD41();
     bool warn = false;
     bool gasDangerAlert = false;
@@ -3510,7 +3533,7 @@ void loop() {
     if(sosState==0){
       
             digitalWrite(siren_led, LOW);
-      if (o2_val < 19.5f || o2_val > 23.6f) {
+      if ((zceGasSamples.ready() && zceGasSamples.average(ZceGasSamples::O2) < 19.5f) || (zceGasSamples.ready() && zceGasSamples.average(ZceGasSamples::O2) > 23.6f)) {
             warn = true;
             
             digitalWrite(siren_led, HIGH);
@@ -3521,14 +3544,14 @@ void loop() {
               delay(300);
 
             }
-            audioPlayByIndex(3);   // 0003.mp3: ?�소부�?
+            playSampledSensorAlert(3);   // 0003.mp3: ?�소부�?
             //play_music();
           }
           else if (
             (co2_val >=15000.0f) ||
-            (ch4_val >= 1.5f) ||
-            (co_val >= 60.0f) ||
-            (h2s_val >= 20.0f)
+            ((zceGasSamples.ready() && zceGasSamples.average(ZceGasSamples::CH4) >= 1.5f)) ||
+            ((zceGasSamples.ready() && zceGasSamples.average(ZceGasSamples::CO) >= 60.0f)) ||
+            ((zceGasSamples.ready() && zceGasSamples.average(ZceGasSamples::H2S) >= 20.0f))
           ) {
             warn = true;
             
@@ -3539,7 +3562,7 @@ void loop() {
               delay(300);
 
             }
-            audioPlayByIndex(5);   // 0005.mp3: ?�해가?�감지
+            playSampledSensorAlert(5);   // 0005.mp3: ?�해가?�감지
             //play_music();
           }  
           else if (temperature >= 99 ) {
@@ -3552,7 +3575,7 @@ void loop() {
               delay(300);
 
             }
-            audioPlayByIndex(4);   // 0004.mp3:고온
+            playSampledSensorAlert(4);   // 0004.mp3:고온
             //play_music();
           }
           else if (temperature <-10 ) {
@@ -3565,7 +3588,7 @@ void loop() {
               delay(300);
 
             }
-            audioPlayByIndex(2);   // 0002.mp3:?�??
+            playSampledSensorAlert(2);   // 0002.mp3:?�??
             //play_music();
           }
           else if (batteryVoltage <= 10 ) {
@@ -3578,7 +3601,7 @@ void loop() {
             }
             if (battery_check_time == 0 || nowMs - battery_check_time >= LOW_BATTERY_ALERT_INTERVAL_MS) {
               battery_check_time = nowMs;
-              audioPlayByIndex(9);   // 0009.mp3:?�?�압
+              playSampledSensorAlert(9);   // 0009.mp3:?�?�압
             }
             //play_music();
           }
diff --git a/test/offline.cpp b/test/offline.cpp
index 1f3a940..4ea66df 100644
--- a/test/offline.cpp
+++ b/test/offline.cpp
@@ -1,3 +1,6 @@
+#include "../include/ZceGasSamples.h"
+static ZceGasSamples zceGasSamples;
+
 #include <Arduino.h>
 #include <Preferences.h>
 #include <Audio.h>
@@ -615,6 +618,8 @@ uint8_t zceChecksum(const uint8_t* buf, size_t len) {
 }
 
 bool readZCE04B() {
+  if (!zceGasSamples.sampleDue(millis())) return false;
+  bool received = false;
   selectGasUart();
 
   while (gasSerial.available() > 0) {
@@ -623,10 +628,10 @@ bool readZCE04B() {
       continue;
     }
 
-    if (gasSerial.available() < ZCE_FRAME_LEN) return false;
+    if (gasSerial.available() < ZCE_FRAME_LEN) break;
 
     size_t n = gasSerial.readBytes(zceFrame, ZCE_FRAME_LEN);
-    if (n != ZCE_FRAME_LEN) return false;
+    if (n != ZCE_FRAME_LEN) break;
 
     if (zceFrame[0] != 0xFF || zceFrame[1] != 0x86) continue;
     if (zceChecksum(zceFrame, ZCE_FRAME_LEN) != zceFrame[10]) continue;
@@ -640,10 +645,11 @@ bool readZCE04B() {
     h2s_val = static_cast<float>(h2sRaw);
     o2_val = static_cast<float>(o2Raw) * 0.1f;
     ch4_val = static_cast<float>(ch4Raw);
-    return true;
+    received = true;
   }
 
-  return false;
+  if (received) zceGasSamples.add(co_val, h2s_val, o2_val, ch4_val);
+  return received;
 }
 
 void readSCD41() {
@@ -698,21 +704,35 @@ void updateDisplay() {
   lcd.print(line);
 }
 
+// Sampling and alarm evaluation run each second; audio repeats every five seconds.
+void playSampledSensorAlert(uint8_t track) {
+  static bool played = false;
+  static uint8_t lastTrack = 0;
+  static uint32_t lastPlaybackMs = 0;
+  const uint32_t nowMs = millis();
+  if (!played || track != lastTrack || uint32_t(nowMs - lastPlaybackMs) >= 5000) {
+    JQ6500_playByIndex(track);
+    played = true;
+    lastTrack = track;
+    lastPlaybackMs = nowMs;
+  }
+}
+
 void serviceAlerts() {
   bool warn = false;
 
   if (!sosState) {
     digitalWrite(SIREN_LED_PIN, LOW);
 
-    if (o2_val < O2_LOW_THRESHOLD || o2_val > O2_HIGH_THRESHOLD) {
+    if ((zceGasSamples.ready() && zceGasSamples.average(ZceGasSamples::O2) < O2_LOW_THRESHOLD) || (zceGasSamples.ready() && zceGasSamples.average(ZceGasSamples::O2) > O2_HIGH_THRESHOLD)) {
       warn = true;
       digitalWrite(SIREN_LED_PIN, HIGH);
       ensureAlertVolume();
       audioPlayByIndex(3);
     } else if (co2_val >= CO2_HIGH_THRESHOLD ||
-               ch4_val >= CH4_HIGH_THRESHOLD ||
-               co_val >= CO_HIGH_THRESHOLD ||
-               h2s_val >= H2S_HIGH_THRESHOLD) {
+               (zceGasSamples.ready() && zceGasSamples.average(ZceGasSamples::CH4) >= CH4_HIGH_THRESHOLD) ||
+               (zceGasSamples.ready() && zceGasSamples.average(ZceGasSamples::CO) >= CO_HIGH_THRESHOLD) ||
+               (zceGasSamples.ready() && zceGasSamples.average(ZceGasSamples::H2S) >= H2S_HIGH_THRESHOLD)) {
       warn = true;
       digitalWrite(SIREN_LED_PIN, HIGH);
       ensureAlertVolume();
@@ -777,6 +797,7 @@ void setup() {
 }
 
 void loop() {
+  readZCE04B();  // Independent 1-second gas sampling, before slower alert/network work.
   handleBatteryCalibrationSerialInput();
   const uint32_t nowMs = millis();
   serviceAudioLoop();
@@ -798,8 +819,8 @@ void loop() {
 
   serviceSosSequence(nowMs);
 
-  if ((nowMs - lastUpdateMs) >= SENSOR_ALERT_REPEAT_MS) {
-    readZCE04B();
+  if ((nowMs - lastUpdateMs) >= ZceGasSamples::SAMPLE_INTERVAL_MS) {
+
     readSCD41();
     batteryPercent = readBatteryVoltage();
     serviceAlerts();
diff --git a/test/only_ethernet.cpp b/test/only_ethernet.cpp
index f304d07..3819963 100644
--- a/test/only_ethernet.cpp
+++ b/test/only_ethernet.cpp
@@ -1,3 +1,6 @@
+#include "../include/ZceGasSamples.h"
+static ZceGasSamples zceGasSamples;
+
 #include <Arduino.h>
 #include <Audio.h>
 #include <LittleFS.h>
@@ -725,6 +728,7 @@ void calibrateZceZero(const char* gasName, const uint8_t* switchCommand) {
 }
 
 void calibrateO2AirSpan20_9() {
+  zceGasSamples.reset();
   Serial.println("O2 air span calibration start");
   switchZceGas(ZCE_SWITCH_TO_O2);
   sendZceCommand(ZCE_O2_AIR_SPAN_20_9, ZCE_CMD_LEN);
@@ -732,6 +736,7 @@ void calibrateO2AirSpan20_9() {
 }
 
 void calibrateAllGasInCleanAir() {
+  zceGasSamples.reset();
   Serial.println("All gas calibration start");
   Serial.println("Keep the sensor in clean normal air. Target: O2=20.9%, CO=0ppm, H2S=0ppm, CH4=0%LEL");
 
@@ -745,9 +750,11 @@ void calibrateAllGasInCleanAir() {
 }
 
 bool readZCE04B() {
+  if (!zceGasSamples.sampleDue(millis())) return false;
+  bool received = false;
   while (gasSerial.available() > 0) {
     if (gasSerial.peek() != 0xFF) { gasSerial.read(); continue; }
-    if (gasSerial.available() < ZCE_FRAME_LEN) return false;
+    if (gasSerial.available() < ZCE_FRAME_LEN) break;
     gasSerial.readBytes(zceFrame, ZCE_FRAME_LEN);
     if (zceFrame[0] != 0xFF || zceFrame[1] != 0x86) continue;
     if (zceChecksum(zceFrame, ZCE_FRAME_LEN) != zceFrame[10]) continue;
@@ -755,9 +762,10 @@ bool readZCE04B() {
     h2s_val = float((uint16_t(zceFrame[4]) << 8) | zceFrame[5]);
     o2_val  = float((uint16_t(zceFrame[6]) << 8) | zceFrame[7]) * 0.1f;
     ch4_val = float((uint16_t(zceFrame[8]) << 8) | zceFrame[9]);
-    return true;
+    received = true;
   }
-  return false;
+  if (received) zceGasSamples.add(co_val, h2s_val, o2_val, ch4_val);
+  return received;
 }
 
 void readSCD41() {
@@ -1201,6 +1209,7 @@ unsigned long lastServerSendMs = 0;
 bool firstSend = true;
 
 void loop() {
+  readZCE04B();  // Independent 1-second gas sampling, before slower alert/network work.
   handleSerialConfigInput();
   const uint32_t nowMs = millis();
   serviceAudioLoop();
@@ -1222,8 +1231,8 @@ void loop() {
   serviceSosSequence(nowMs);
 
   // Sensor Reading & LCD Update
-  if (nowMs - lastUpdate >= SENSOR_ALERT_REPEAT_MS) {
-    readZCE04B(); readSCD41();
+  if (nowMs - lastUpdate >= ZceGasSamples::SAMPLE_INTERVAL_MS) {
+     readSCD41();
     batteryVoltage = readBatteryVoltage();
     
     if (hasLcd) {
@@ -1246,7 +1255,7 @@ void loop() {
   // Periodic Sending
   if (netMode == ETHERNET_MODE) {
     bool periodic = (nowMs - lastServerSendMs >= SERVER_SEND_PERIODIC_INTERVAL_MS);
-    bool threshold = (co2_val >= serverSendThresholds.co2 || ch4_val >= serverSendThresholds.ch4 || o2_val < serverSendThresholds.o2Low); // simplified
+    bool threshold = (co2_val >= serverSendThresholds.co2 || (zceGasSamples.ready() && zceGasSamples.average(ZceGasSamples::CH4) >= serverSendThresholds.ch4) || (zceGasSamples.ready() && zceGasSamples.average(ZceGasSamples::O2) < serverSendThresholds.o2Low)); // simplified
     if ((firstSend || periodic || threshold || sosState == 1) && (nowMs - lastServerSendMs >= SERVER_SEND_MIN_INTERVAL_MS)) {
       if (sendToServer()) {
         lastServerSendMs = nowMs; firstSend = false;
diff --git a/test/origin.cpp b/test/origin.cpp
index feea8c6..1f756a4 100644
--- a/test/origin.cpp
+++ b/test/origin.cpp
@@ -1,3 +1,6 @@
+#include "../include/ZceGasSamples.h"
+static ZceGasSamples zceGasSamples;
+
 #include <Arduino.h>
 #include <Audio.h>
 #include <LittleFS.h>
@@ -3426,20 +3429,20 @@ bool sendToServerAndRemember() {
 bool isGasDangerAlertCondition() {
   return
       (co2_val >= 15000.0f) ||
-      (ch4_val >= 1.5f) ||
-      (o2MonitoringReady && (o2_val < 19.5f || o2_val > 23.6f)) ||
-      (co_val >= 60.0f) ||
-      (h2s_val >= 20.0f);
+      ((zceGasSamples.ready() && zceGasSamples.average(ZceGasSamples::CH4) >= 1.5f)) ||
+      (o2MonitoringReady && ((zceGasSamples.ready() && zceGasSamples.average(ZceGasSamples::O2) < 19.5f) || (zceGasSamples.ready() && zceGasSamples.average(ZceGasSamples::O2) > 23.6f))) ||
+      ((zceGasSamples.ready() && zceGasSamples.average(ZceGasSamples::CO) >= 60.0f)) ||
+      ((zceGasSamples.ready() && zceGasSamples.average(ZceGasSamples::H2S) >= 20.0f));
 }
 
 bool shouldSendToServerForThreshold() {
   return
       (co2_val >= serverSendThresholds.co2) ||
-      (ch4_val >= serverSendThresholds.ch4) ||
-      (o2MonitoringReady && (o2_val < serverSendThresholds.o2Low ||
-                             o2_val > serverSendThresholds.o2High)) ||
-      (co_val >= serverSendThresholds.co) ||
-      (h2s_val >= serverSendThresholds.h2s) ||
+      ((zceGasSamples.ready() && zceGasSamples.average(ZceGasSamples::CH4) >= serverSendThresholds.ch4)) ||
+      (o2MonitoringReady && ((zceGasSamples.ready() && zceGasSamples.average(ZceGasSamples::O2) < serverSendThresholds.o2Low) ||
+                             (zceGasSamples.ready() && zceGasSamples.average(ZceGasSamples::O2) > serverSendThresholds.o2High))) ||
+      ((zceGasSamples.ready() && zceGasSamples.average(ZceGasSamples::CO) >= serverSendThresholds.co)) ||
+      ((zceGasSamples.ready() && zceGasSamples.average(ZceGasSamples::H2S) >= serverSendThresholds.h2s)) ||
       (temperature >= serverSendThresholds.tempHigh) ||
       (temperature <= serverSendThresholds.tempLow);
 }
@@ -3460,13 +3463,13 @@ void addAlertTrack(uint8_t* tracks, uint8_t& count, uint8_t trackIndex) {
 void buildAlertTrackQueue(uint8_t* tracks, uint8_t& count) {
   count = 0;
 
-  if (o2MonitoringReady && (o2_val < 19.5f || o2_val > 23.6f)) {
+  if (o2MonitoringReady && ((zceGasSamples.ready() && zceGasSamples.average(ZceGasSamples::O2) < 19.5f) || (zceGasSamples.ready() && zceGasSamples.average(ZceGasSamples::O2) > 23.6f))) {
     addAlertTrack(tracks, count, 3);   // 0003.mp3: oxygen alert
   }
   if ((co2_val >= 15000.0f) ||
-      (ch4_val >= 1.5f) ||
-      (co_val >= 60.0f) ||
-      (h2s_val >= 20.0f)) {
+      ((zceGasSamples.ready() && zceGasSamples.average(ZceGasSamples::CH4) >= 1.5f)) ||
+      ((zceGasSamples.ready() && zceGasSamples.average(ZceGasSamples::CO) >= 60.0f)) ||
+      ((zceGasSamples.ready() && zceGasSamples.average(ZceGasSamples::H2S) >= 20.0f))) {
     addAlertTrack(tracks, count, 5);   // 0005.mp3: hazardous gas alert
   }
   if (temperature >= 99.0f) {
@@ -3711,6 +3714,7 @@ void calibrateZceZero(const char* gasName, const uint8_t* switchCommand) {
 }
 
 void calibrateO2AirSpan20_9() {
+  zceGasSamples.reset();
   Serial.println("O2 air span calibration start");
   switchZceGas(ZCE_SWITCH_TO_O2);
   sendZceCommand(ZCE_O2_AIR_SPAN_20_9, ZCE_CMD_LEN);
@@ -3718,6 +3722,7 @@ void calibrateO2AirSpan20_9() {
 }
 
 void calibrateAllGasInCleanAir() {
+  zceGasSamples.reset();
   Serial.println("All gas calibration start");
   Serial.println("Keep the sensor in clean normal air. Target: O2=20.9%, CO=0ppm, H2S=0ppm, CH4=0%LEL");
 
@@ -3731,6 +3736,8 @@ void calibrateAllGasInCleanAir() {
 }
 
 bool readZCE04B() {
+  if (!zceGasSamples.sampleDue(millis())) return false;
+  bool received = false;
   selectGasUart();
   // ?�리??버퍼?�서 0xFF(?��??? 맞춰???�기
   while (gasSerial.available() > 0) {
@@ -3741,11 +3748,11 @@ bool readZCE04B() {
     }
 
     // ?�레??길이만큼 ???��??�면 ?�음 loop?�서
-    if (gasSerial.available() < ZCE_FRAME_LEN) return false;
+    if (gasSerial.available() < ZCE_FRAME_LEN) break;
 
     // 11바이???�기
     size_t n = gasSerial.readBytes(zceFrame, ZCE_FRAME_LEN);
-    if (n != ZCE_FRAME_LEN) return false;
+    if (n != ZCE_FRAME_LEN) break;
 
     // 기본 검�?
     if (zceFrame[0] != 0xFF) continue;
@@ -3775,10 +3782,11 @@ bool readZCE04B() {
       Serial.printf("[O2] monitoring started at %.1f%%\n", o2_val);
     }
 
-    return true;
+    received = true;
   }
 
-  return false;
+  if (received) zceGasSamples.add(co_val, h2s_val, o2_val, ch4_val);
+  return received;
 }
 
 // void readZCE04B() {  //?�른 ?�서 버전 
@@ -3831,6 +3839,7 @@ void readSCD41() {
 unsigned long lastUpdate = 0;
 bool sensorSnapshotReady = false;
 void loop() {
+  readZCE04B();  // Independent 1-second gas sampling, before slower alert/network work.
   if (ENABLE_AUDIO_VOLUME_TEST) {
     serviceAudioVolumeTest(millis());
     return;
@@ -3862,9 +3871,9 @@ void loop() {
   serviceSosSequence(nowMs);
   serviceSensorAlertSequence(nowMs);
  
-  if (millis() - lastUpdate >= SENSOR_ALERT_REPEAT_MS) {
+  if (millis() - lastUpdate >= ZceGasSamples::SAMPLE_INTERVAL_MS) {
     
-  readZCE04B();
+
   readSCD41();
     bool warn = false;
   
@@ -3898,7 +3907,7 @@ void loop() {
     if(false){
       
             digitalWrite(siren_led, LOW);
-      if (o2_val < 19.5f || o2_val > 23.6f) {
+      if ((zceGasSamples.ready() && zceGasSamples.average(ZceGasSamples::O2) < 19.5f) || (zceGasSamples.ready() && zceGasSamples.average(ZceGasSamples::O2) > 23.6f)) {
             warn = true;
             
             digitalWrite(siren_led, HIGH);
@@ -3909,9 +3918,9 @@ void loop() {
           }
           else if (
             (co2_val >=15000.0f) ||
-            (ch4_val >= 1.5f) ||
-            (co_val >= 60.0f) ||
-            (h2s_val >= 20.0f)
+            ((zceGasSamples.ready() && zceGasSamples.average(ZceGasSamples::CH4) >= 1.5f)) ||
+            ((zceGasSamples.ready() && zceGasSamples.average(ZceGasSamples::CO) >= 60.0f)) ||
+            ((zceGasSamples.ready() && zceGasSamples.average(ZceGasSamples::H2S) >= 20.0f))
           ) {
             warn = true;
             
diff --git a/test/static_test.cpp b/test/static_test.cpp
index b07a326..63087c6 100644
--- a/test/static_test.cpp
+++ b/test/static_test.cpp
@@ -1,3 +1,6 @@
+#include "../include/ZceGasSamples.h"
+static ZceGasSamples zceGasSamples;
+
 #include <Arduino.h>
 #include <Wire.h>
 #include <SPI.h>
@@ -3273,21 +3276,21 @@ bool sendToServerAndRemember() {
 bool isGasDangerAlertCondition() {
   return
       (co2_val >= 15000.0f) ||
-      (ch4_val >= 1.5f) ||
-      (o2_val < 19.5f) ||
-      (o2_val > 23.6f) ||
-      (co_val >= 60.0f) ||
-      (h2s_val >= 20.0f);
+      ((zceGasSamples.ready() && zceGasSamples.average(ZceGasSamples::CH4) >= 1.5f)) ||
+      ((zceGasSamples.ready() && zceGasSamples.average(ZceGasSamples::O2) < 19.5f)) ||
+      ((zceGasSamples.ready() && zceGasSamples.average(ZceGasSamples::O2) > 23.6f)) ||
+      ((zceGasSamples.ready() && zceGasSamples.average(ZceGasSamples::CO) >= 60.0f)) ||
+      ((zceGasSamples.ready() && zceGasSamples.average(ZceGasSamples::H2S) >= 20.0f));
 }
 
 bool shouldSendToServerForThreshold() {
   return
       (co2_val >= serverSendThresholds.co2) ||
-      (ch4_val >= serverSendThresholds.ch4) ||
-      (o2_val < serverSendThresholds.o2Low) ||
-      (o2_val > serverSendThresholds.o2High) ||
-      (co_val >= serverSendThresholds.co) ||
-      (h2s_val >= serverSendThresholds.h2s) ||
+      ((zceGasSamples.ready() && zceGasSamples.average(ZceGasSamples::CH4) >= serverSendThresholds.ch4)) ||
+      ((zceGasSamples.ready() && zceGasSamples.average(ZceGasSamples::O2) < serverSendThresholds.o2Low)) ||
+      ((zceGasSamples.ready() && zceGasSamples.average(ZceGasSamples::O2) > serverSendThresholds.o2High)) ||
+      ((zceGasSamples.ready() && zceGasSamples.average(ZceGasSamples::CO) >= serverSendThresholds.co)) ||
+      ((zceGasSamples.ready() && zceGasSamples.average(ZceGasSamples::H2S) >= serverSendThresholds.h2s)) ||
       (temperature >= serverSendThresholds.tempHigh) ||
       (temperature <= serverSendThresholds.tempLow);
 }
@@ -3460,6 +3463,7 @@ void calibrateZceZero(const char* gasName, const uint8_t* switchCommand) {
 }
 
 void calibrateO2AirSpan20_9() {
+  zceGasSamples.reset();
   Serial.println("O2 air span calibration start");
   switchZceGas(ZCE_SWITCH_TO_O2);
   sendZceCommand(ZCE_O2_AIR_SPAN_20_9, ZCE_CMD_LEN);
@@ -3467,6 +3471,7 @@ void calibrateO2AirSpan20_9() {
 }
 
 void calibrateAllGasInCleanAir() {
+  zceGasSamples.reset();
   Serial.println("All gas calibration start");
   Serial.println("Keep the sensor in clean normal air. Target: O2=20.9%, CO=0ppm, H2S=0ppm, CH4=0%LEL");
 
@@ -3481,6 +3486,8 @@ void calibrateAllGasInCleanAir() {
 
 // ?꾨젅??1�????�� ?깃났??�㈃ true
 bool readZCE04B() {
+  if (!zceGasSamples.sampleDue(millis())) return false;
+  bool received = false;
   selectGasUart();
   // ??�━??踰꾪??�?�� 0xFF(????? 留욎?????�린
   while (gasSerial.available() > 0) {
@@ -3491,11 +3498,11 @@ bool readZCE04B() {
     }
 
     // ?꾨젅??湲몄?�留?�겮 ???�???�㈃ ??�쓬 loop?�?��
-    if (gasSerial.available() < ZCE_FRAME_LEN) return false;
+    if (gasSerial.available() < ZCE_FRAME_LEN) break;
 
     // 11諛붿?????�린
     size_t n = gasSerial.readBytes(zceFrame, ZCE_FRAME_LEN);
-    if (n != ZCE_FRAME_LEN) return false;
+    if (n != ZCE_FRAME_LEN) break;
 
     // 湲곕??寃�?
     if (zceFrame[0] != 0xFF) continue;
@@ -3521,10 +3528,11 @@ bool readZCE04B() {
     o2_val  = float(o2_raw) * 0.1f;   // %vol
     ch4_val = float(ch4_raw);         // %LEL
 
-    return true;
+    received = true;
   }
 
-  return false;
+  if (received) zceGasSamples.add(co_val, h2s_val, o2_val, ch4_val);
+  return received;
 }
 
 // void readZCE04B() {  //??�Ⅸ ??�꽌 踰꾩??
@@ -3576,7 +3584,22 @@ void readSCD41() {
 // 痢≪??二쇨�?
 unsigned long lastUpdate = 0;
 bool sensorSnapshotReady = false;
+// Sampling and alarm evaluation run each second; audio repeats every five seconds.
+void playSampledSensorAlert(uint8_t track) {
+  static bool played = false;
+  static uint8_t lastTrack = 0;
+  static uint32_t lastPlaybackMs = 0;
+  const uint32_t nowMs = millis();
+  if (!played || track != lastTrack || uint32_t(nowMs - lastPlaybackMs) >= 5000) {
+    audioPlayByIndex(track);
+    played = true;
+    lastTrack = track;
+    lastPlaybackMs = nowMs;
+  }
+}
+
 void loop() {
+  readZCE04B();  // Independent 1-second gas sampling, before slower alert/network work.
 #if ETHERNET_ENABLE_DHCP_FALLBACK
   if (netMode == ETHERNET_MODE) {
     if (!ethernetUsingStaticIp) {
@@ -3601,9 +3624,9 @@ void loop() {
   }
   serviceSosSequence(nowMs);
  
-  if (millis() - lastUpdate >= SENSOR_ALERT_REPEAT_MS) {
+  if (millis() - lastUpdate >= ZceGasSamples::SAMPLE_INTERVAL_MS) {
     
-  readZCE04B();
+
   readSCD41();
     bool warn = false;
     bool gasDangerAlert = false;
@@ -3615,7 +3638,7 @@ void loop() {
     if(sosState==0){
       
             digitalWrite(siren_led, LOW);
-      if (o2_val < 19.5f || o2_val > 23.6f) {
+      if ((zceGasSamples.ready() && zceGasSamples.average(ZceGasSamples::O2) < 19.5f) || (zceGasSamples.ready() && zceGasSamples.average(ZceGasSamples::O2) > 23.6f)) {
             warn = true;
             
             digitalWrite(siren_led, HIGH);
@@ -3626,14 +3649,14 @@ void loop() {
               delay(300);
 
             }
-            audioPlayByIndex(3);   // 0003.mp3: ?곗냼?��?
+            playSampledSensorAlert(3);   // 0003.mp3: ?곗냼?��?
             //play_music();
           }
           else if (
             (co2_val >=15000.0f) ||
-            (ch4_val >= 1.5f) ||
-            (co_val >= 60.0f) ||
-            (h2s_val >= 20.0f)
+            ((zceGasSamples.ready() && zceGasSamples.average(ZceGasSamples::CH4) >= 1.5f)) ||
+            ((zceGasSamples.ready() && zceGasSamples.average(ZceGasSamples::CO) >= 60.0f)) ||
+            ((zceGasSamples.ready() && zceGasSamples.average(ZceGasSamples::H2S) >= 20.0f))
           ) {
             warn = true;
             
@@ -3644,7 +3667,7 @@ void loop() {
               delay(300);
 
             }
-            audioPlayByIndex(5);   // 0005.mp3: ?좏빐媛??�컧吏
+            playSampledSensorAlert(5);   // 0005.mp3: ?좏빐媛??�컧吏
             //play_music();
           }  
           else if (temperature >= 99 ) {
@@ -3657,7 +3680,7 @@ void loop() {
               delay(300);
 
             }
-            audioPlayByIndex(4);   // 0004.mp3:?�좎??
+            playSampledSensorAlert(4);   // 0004.mp3:?�좎??
             //play_music();
           }
           else if (temperature <-10 ) {
@@ -3670,7 +3693,7 @@ void loop() {
               delay(300);
 
             }
-            audioPlayByIndex(2);   // 0002.mp3:????
+            playSampledSensorAlert(2);   // 0002.mp3:????
             //play_music();
           }
           else if (batteryVoltage <= 10 ) {
@@ -3683,7 +3706,7 @@ void loop() {
             }
             if (battery_check_time == 0 || nowMs - battery_check_time >= LOW_BATTERY_ALERT_INTERVAL_MS) {
               battery_check_time = nowMs;
-              audioPlayByIndex(9);   // 0009.mp3:???꾩븬
+              playSampledSensorAlert(9);   // 0009.mp3:???꾩븬
             }
             //play_music();
           }
diff --git a/test/wifi_version.cpp b/test/wifi_version.cpp
index b030cd6..8b11714 100644
--- a/test/wifi_version.cpp
+++ b/test/wifi_version.cpp
@@ -1,3 +1,6 @@
+#include "../include/ZceGasSamples.h"
+static ZceGasSamples zceGasSamples;
+
 #include <Arduino.h>
 #include <Audio.h>
 #include <LittleFS.h>
@@ -3299,21 +3302,21 @@ bool sendToServerAndRemember() {
 bool isGasDangerAlertCondition() {
   return
       (co2_val >= 15000.0f) ||
-      (ch4_val >= 1.5f) ||
-      (o2_val < 19.5f) ||
-      (o2_val > 23.6f) ||
-      (co_val >= 60.0f) ||
-      (h2s_val >= 20.0f);
+      ((zceGasSamples.ready() && zceGasSamples.average(ZceGasSamples::CH4) >= 1.5f)) ||
+      ((zceGasSamples.ready() && zceGasSamples.average(ZceGasSamples::O2) < 19.5f)) ||
+      ((zceGasSamples.ready() && zceGasSamples.average(ZceGasSamples::O2) > 23.6f)) ||
+      ((zceGasSamples.ready() && zceGasSamples.average(ZceGasSamples::CO) >= 60.0f)) ||
+      ((zceGasSamples.ready() && zceGasSamples.average(ZceGasSamples::H2S) >= 20.0f));
 }
 
 bool shouldSendToServerForThreshold() {
   return
       (co2_val >= serverSendThresholds.co2) ||
-      (ch4_val >= serverSendThresholds.ch4) ||
-      (o2_val < serverSendThresholds.o2Low) ||
-      (o2_val > serverSendThresholds.o2High) ||
-      (co_val >= serverSendThresholds.co) ||
-      (h2s_val >= serverSendThresholds.h2s) ||
+      ((zceGasSamples.ready() && zceGasSamples.average(ZceGasSamples::CH4) >= serverSendThresholds.ch4)) ||
+      ((zceGasSamples.ready() && zceGasSamples.average(ZceGasSamples::O2) < serverSendThresholds.o2Low)) ||
+      ((zceGasSamples.ready() && zceGasSamples.average(ZceGasSamples::O2) > serverSendThresholds.o2High)) ||
+      ((zceGasSamples.ready() && zceGasSamples.average(ZceGasSamples::CO) >= serverSendThresholds.co)) ||
+      ((zceGasSamples.ready() && zceGasSamples.average(ZceGasSamples::H2S) >= serverSendThresholds.h2s)) ||
       (temperature >= serverSendThresholds.tempHigh) ||
       (temperature <= serverSendThresholds.tempLow);
 }
@@ -3494,6 +3497,7 @@ void calibrateZceZero(const char* gasName, const uint8_t* switchCommand) {
 }
 
 void calibrateO2AirSpan20_9() {
+  zceGasSamples.reset();
   Serial.println("O2 air span calibration start");
   switchZceGas(ZCE_SWITCH_TO_O2);
   sendZceCommand(ZCE_O2_AIR_SPAN_20_9, ZCE_CMD_LEN);
@@ -3501,6 +3505,7 @@ void calibrateO2AirSpan20_9() {
 }
 
 void calibrateAllGasInCleanAir() {
+  zceGasSamples.reset();
   Serial.println("All gas calibration start");
   Serial.println("Keep the sensor in clean normal air. Target: O2=20.9%, CO=0ppm, H2S=0ppm, CH4=0%LEL");
 
@@ -3514,6 +3519,8 @@ void calibrateAllGasInCleanAir() {
 }
 
 bool readZCE04B() {
+  if (!zceGasSamples.sampleDue(millis())) return false;
+  bool received = false;
   selectGasUart();
   // 시리얼 버퍼에서 0xFF(스타트) 맞춰서 읽기
   while (gasSerial.available() > 0) {
@@ -3524,11 +3531,11 @@ bool readZCE04B() {
     }
 
     // 프레임 길이만큼 안 쌓였으면 다음 loop에서
-    if (gasSerial.available() < ZCE_FRAME_LEN) return false;
+    if (gasSerial.available() < ZCE_FRAME_LEN) break;
 
     // 11바이트 읽기
     size_t n = gasSerial.readBytes(zceFrame, ZCE_FRAME_LEN);
-    if (n != ZCE_FRAME_LEN) return false;
+    if (n != ZCE_FRAME_LEN) break;
 
     // 기본 검증
     if (zceFrame[0] != 0xFF) continue;
@@ -3554,10 +3561,11 @@ bool readZCE04B() {
     o2_val  = float(o2_raw) * 0.1f;   // %vol
     ch4_val = float(ch4_raw);         // %LEL
 
-    return true;
+    received = true;
   }
 
-  return false;
+  if (received) zceGasSamples.add(co_val, h2s_val, o2_val, ch4_val);
+  return received;
 }
 
 // void readZCE04B() {  //다른 센서 버전 
@@ -3609,7 +3617,22 @@ void readSCD41() {
 // 측정 주기
 unsigned long lastUpdate = 0;
 bool sensorSnapshotReady = false;
+// Sampling and alarm evaluation run each second; audio repeats every five seconds.
+void playSampledSensorAlert(uint8_t track) {
+  static bool played = false;
+  static uint8_t lastTrack = 0;
+  static uint32_t lastPlaybackMs = 0;
+  const uint32_t nowMs = millis();
+  if (!played || track != lastTrack || uint32_t(nowMs - lastPlaybackMs) >= 5000) {
+    audioPlayByIndex(track);
+    played = true;
+    lastTrack = track;
+    lastPlaybackMs = nowMs;
+  }
+}
+
 void loop() {
+  readZCE04B();  // Independent 1-second gas sampling, before slower alert/network work.
   handleSerialConfigInput();
   const uint32_t nowMs = millis();
   serviceAudioLoop();
@@ -3635,9 +3658,9 @@ void loop() {
   }
   serviceSosSequence(nowMs);
  
-  if (millis() - lastUpdate >= SENSOR_ALERT_REPEAT_MS) {
+  if (millis() - lastUpdate >= ZceGasSamples::SAMPLE_INTERVAL_MS) {
     
-  readZCE04B();
+
   readSCD41();
     bool warn = false;
     bool gasDangerAlert = false;
@@ -3649,7 +3672,7 @@ void loop() {
     if(sosState==0){
       
             digitalWrite(siren_led, LOW);
-      if (o2_val < 19.5f || o2_val > 23.6f) {
+      if ((zceGasSamples.ready() && zceGasSamples.average(ZceGasSamples::O2) < 19.5f) || (zceGasSamples.ready() && zceGasSamples.average(ZceGasSamples::O2) > 23.6f)) {
             warn = true;
             
             digitalWrite(siren_led, HIGH);
@@ -3660,14 +3683,14 @@ void loop() {
               delay(300);
 
             }
-            audioPlayByIndex(3);   // 0003.mp3: 산소부족
+            playSampledSensorAlert(3);   // 0003.mp3: 산소부족
             //play_music();
           }
           else if (
             (co2_val >=15000.0f) ||
-            (ch4_val >= 1.5f) ||
-            (co_val >= 60.0f) ||
-            (h2s_val >= 20.0f)
+            ((zceGasSamples.ready() && zceGasSamples.average(ZceGasSamples::CH4) >= 1.5f)) ||
+            ((zceGasSamples.ready() && zceGasSamples.average(ZceGasSamples::CO) >= 60.0f)) ||
+            ((zceGasSamples.ready() && zceGasSamples.average(ZceGasSamples::H2S) >= 20.0f))
           ) {
             warn = true;
             
@@ -3678,7 +3701,7 @@ void loop() {
               delay(300);
 
             }
-            audioPlayByIndex(5);   // 0005.mp3: 유해가스감지
+            playSampledSensorAlert(5);   // 0005.mp3: 유해가스감지
             //play_music();
           }  
           else if (temperature >= 99 ) {
@@ -3691,7 +3714,7 @@ void loop() {
               delay(300);
 
             }
-            audioPlayByIndex(4);   // 0004.mp3:고온
+            playSampledSensorAlert(4);   // 0004.mp3:고온
             //play_music();
           }
           else if (temperature <-10 ) {
@@ -3704,7 +3727,7 @@ void loop() {
               delay(300);
 
             }
-            audioPlayByIndex(2);   // 0002.mp3:저온
+            playSampledSensorAlert(2);   // 0002.mp3:저온
             //play_music();
           }
           else if (batteryVoltage <= 10 ) {
@@ -3717,7 +3740,7 @@ void loop() {
             }
             if (battery_check_time == 0 || nowMs - battery_check_time >= LOW_BATTERY_ALERT_INTERVAL_MS) {
               battery_check_time = nowMs;
-              audioPlayByIndex(9);   // 0009.mp3:저전압
+              playSampledSensorAlert(9);   // 0009.mp3:저전압
             }
             //play_music();
           }
